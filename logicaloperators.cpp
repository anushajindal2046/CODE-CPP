#include <iostream>
using namespace std;
int main()
{
    //logical operator 
    cout << ((3 < 5) && (10 > 5) ) <<endl; // true -> 1
    //If either of the situation is false. So,the result will be false. 
    cout << ((3 < 5) && (10 == 5) ) <<endl; //false -> 0
}